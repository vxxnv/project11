@{
    ViewData["Title"] = "Восстановление пароля";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header bg-warning text-dark">
                    <h2 class="text-center mb-0"><i class="fas fa-key me-2"></i>@ViewData["Title"]</h2>
                </div>
                <div class="card-body p-4">
                    <p class="text-muted mb-4">
                        Введите ваш email адрес, и мы вышлем вам ссылку для восстановления пароля.
                    </p>
                    
                    <form method="post" action="/Identity/Account/ForgotPassword">
                        <div class="form-group mb-4">
                            <label class="form-label">Email</label>
                            <input type="email" name="Input.Email" class="form-control" placeholder="Введите ваш email" required />
                        </div>
                        
                        <div class="form-group mb-3">
                            <button type="submit" class="btn btn-warning w-100 btn-lg">
                                <i class="fas fa-paper-plane me-2"></i>Отправить ссылку для восстановления
                            </button>
                        </div>
                        
                        <div class="text-center">
                            <p class="mb-2">
                                <a href="/Account/Login"><i class="fas fa-arrow-left me-1"></i>Вернуться к входу</a>
                            </p>
                            <p class="mb-0">
                                <a href="/Account/Register">Создать новый аккаунт</a>
                            </p>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>